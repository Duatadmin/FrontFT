# nixpacks.toml

# Specify that a Node provider is needed
providers = ["node"]

[phases.setup]
# Use Node 20 LTS
nixPkgs = ["nodejs_20"]

[phases.install]
cmds = ["npm ci"]

[phases.build]
cmds = ["npm run build"]

[phases.start]
cmd = "npx serve -s dist -l $PORT"
